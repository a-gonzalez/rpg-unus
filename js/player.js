export default class Player
{
    constructor()
    {
        console.log(`${this.constructor.name}.ctor @ ${new Date().toLocaleString()}`);
    }

    draw(context)
    {
        
    }

    update(delta_time)
    {

    }
}